workflow: ContinuousDelivery
assembly-versioning-scheme: MajorMinorPatch
tag-prefix: '[vV]'
next-version: 0.2.0
branches:
  main:
    regex: ^main$
    label: ''
    increment: Patch
    source-branches:
      - feature
      - hotfix
      - release
  feature:
    regex: ^feature[/-]
    label: alpha
    increment: Minor
    source-branches:
      - main
  hotfix:
    regex: ^hotfix[/-]
    label: beta
    increment: Patch
    source-branches:
      - main
  release:
    regex: ^release[/-]
    label: rc
    increment: None
    source-branches:
      - main
      - feature
      - hotfix
